/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@mindforge/growth', '@nexus/config'],
  // ... resto de tu configuración si existe
};

module.exports = nextConfig;